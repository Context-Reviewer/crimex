"""
crimex.ui

Phase 0 UI: a deterministic, read-only run viewer served locally.

Design constraints:
- No writes to the run directory
- No external dependencies (stdlib only)
- Deterministic listing and summaries
"""
